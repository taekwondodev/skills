# Delivery policy

- Default mode: `<direct or pr>`.
- Integration target: `<repository, remote, and branch that receive completed work>`.
- PR base: `<target repository, remote, and base branch>`.
- PR source: `<source remote or explicit fork-selection rule>`.
- Required verification: `<existing local checks and remote gates, or a pointer to their authoritative configuration>`.

Before selecting the route or preparing a branch, read the shared delivery reference supplied by `implement` (`references/delivery.md`).
