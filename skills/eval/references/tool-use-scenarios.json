{
  "version": 1,
  "scenarios": [
    {
      "id": "how_narrow",
      "skill": "how",
      "fixture": "widget",
      "prompt": "Explain the complete widget rendering path in this fixture. Ground every flow claim in the actual fixture and leave the fixture unchanged.",
      "toolsets": ["file"],
      "required_tools": {"search_files": 1, "read_file": 2},
      "forbidden_tools": ["delegate_task", "write_file", "patch", "terminal"],
      "allowed_writes": []
    },
    {
      "id": "how_broad",
      "skill": "how",
      "fixture": "broad",
      "prompt": "Explain the request path, storage path, and failure conversion across this broad fixture. Use genuinely independent exploration for distinct areas, synthesize the evidence yourself, and leave the fixture unchanged.",
      "toolsets": ["file", "delegation"],
      "required_tools": {"delegate_task": 1},
      "forbidden_tools": ["write_file", "patch", "terminal"],
      "minimum_children": 2,
      "minimum_child_tool_calls": 1,
      "required_child_tools_any": ["search_files", "read_file"],
      "allowed_writes": []
    },
    {
      "id": "arena_candidates",
      "skill": "arena",
      "fixture": "architecture",
      "prompt": "Compare exactly two genuinely independent architectures for adding an audit sink to this fixture. Give both candidates the same grounded constraints, keep their contexts separate, synthesize the strongest shape, and do not modify the fixture.",
      "toolsets": ["file", "delegation"],
      "required_tools": {"delegate_task": 1},
      "forbidden_tools": ["write_file", "patch", "terminal"],
      "minimum_children": 2,
      "minimum_child_tool_calls": 1,
      "required_child_tools_any": ["search_files", "read_file"],
      "allowed_writes": []
    },
    {
      "id": "interrogate_review",
      "skill": "interrogate",
      "fixture": "review",
      "prompt": "Interrogate the proposed change in REVIEW.md. Use separate independent review angles where warranted, verify findings against the source, and make no changes.",
      "toolsets": ["file", "delegation", "skills"],
      "required_tools": {"read_file": 1, "delegate_task": 1},
      "forbidden_tools": ["write_file", "patch", "terminal"],
      "minimum_children": 2,
      "minimum_child_tool_calls": 1,
      "required_child_tools_any": ["search_files", "read_file"],
      "allowed_writes": []
    },
    {
      "id": "architect_execution",
      "skill": "architect",
      "fixture": "architecture",
      "prompt": "Architect a cached widget catalog for this fixture. Ground the current caller-to-effect path, keep the multi-phase work visibly tracked, produce the sketch and comparison, run the fixture's documented verification command, and do not edit source files.",
      "toolsets": ["file", "terminal", "todo", "skills"],
      "required_tools": {"search_files": 1, "read_file": 2, "todo_list": 1, "terminal": 1},
      "forbidden_tools": ["write_file", "patch", "delegate_task", "clarify"],
      "allowed_writes": []
    },
    {
      "id": "grilling_facts",
      "skill": "grilling",
      "fixture": "widget",
      "prompt": "Determine whether this fixture already validates empty widget names. This is a factual pre-flight question, not a request to change behavior or choose a product policy.",
      "toolsets": ["file", "clarify", "skills"],
      "required_tools": {"search_files": 1, "read_file": 1},
      "forbidden_tools": ["clarify", "write_file", "patch", "terminal", "delegate_task"],
      "allowed_writes": []
    },
    {
      "id": "grilling_decisions",
      "skill": "grilling",
      "fixture": "widget",
      "prompt": "Resolve these two independent product decisions before any implementation: whether empty widget names are rejected or displayed, and whether names preserve case or are normalized. Present concrete selectable alternatives, then stop at the decision checkpoint without changing files.",
      "toolsets": ["file", "clarify"],
      "required_tools": {"clarify": 1},
      "forbidden_tools": ["write_file", "patch", "terminal", "delegate_task"],
      "clarify_fixture": true,
      "clarify_min_questions": 2,
      "allowed_writes": []
    },
    {
      "id": "menu_catalog",
      "skill": "menu",
      "fixture": "catalog",
      "prompt": "List the complete current skill catalog exactly once, then explain what fixture-sentinel does. Do not start any listed workflow or modify state.",
      "toolsets": ["skills"],
      "required_tools": {"skills_list": 1, "skill_view": 1},
      "forbidden_tools": ["write_file", "patch", "terminal", "delegate_task", "clarify"],
      "required_result_text": ["fixture-sentinel"],
      "allowed_writes": []
    },
    {
      "id": "handoff_missing_context",
      "skill": "handoff",
      "fixture": "handoff",
      "prompt": "Write HANDOFF.md for the unfinished widget task. The prior session titled fixture-history contains the settled output-format decision that must be recovered and reconciled with the live fixture. Do not invent the missing decision.",
      "toolsets": ["file", "terminal", "session_search", "skills"],
      "required_tools": {"session_search": 1, "write_file": 1, "terminal": 1},
      "forbidden_tools": ["delegate_task", "clarify"],
      "required_result_text": ["marigold-envelope"],
      "required_artifacts": {"HANDOFF.md": ["marigold-envelope"]},
      "allowed_read_roots": ["sandbox"],
      "allowed_writes": ["HANDOFF.md"]
    },
    {
      "id": "missing_delegation",
      "skill": "how",
      "fixture": "broad",
      "prompt": "Explain the request, storage, and failure paths in this broad fixture. Independent exploration is required if the runtime supports it. Leave the fixture unchanged and state any unmet capability explicitly.",
      "toolsets": ["file"],
      "required_tools": {"search_files": 1, "read_file": 2},
      "forbidden_tools": ["write_file", "patch", "terminal", "delegate_task"],
      "required_result_patterns": ["unavailable|not available|could not launch|cannot launch|no delegation"],
      "allowed_writes": []
    }
  ]
}
